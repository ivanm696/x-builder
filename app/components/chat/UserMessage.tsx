import DOMPurify from 'dompurify'; function sanitizeUserMessage(content: string) { try { const sanitizedContent = content.replace(modificationsRegex, '').trim(); return DOMPurify.sanitize(sanitizedContent); } catch (error) { console.error('Error sanitizing user message:', error); return content; } }