import DOMPurify from 'dompurify'; function sanitizeUserMessage(content: string) { try { const sanitized = content.replace(modificationsRegex, '').trim(); return DOMPurify.sanitize(sanitized); } catch (error) { console.error('Error sanitizing user message:', error); return content; } }